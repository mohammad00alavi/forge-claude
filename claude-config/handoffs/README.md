Per-venture handoff ledgers live here as <slug>.md — the contract passed
between agents (see the forge-playbook skill). Section visibility rules
apply: the verifier sees only the spec + artifact, never the builder's notes.
