---
description: Sharpen a fuzzy idea into a crisp value prop, target user, core feature, and non-goals — $ARGUMENTS
---

Brainstorm and sharpen: $ARGUMENTS

Hand off to `strategist` (+ `pm` if the tier is T2+). Produce:
- One-line value proposition a stranger could repeat.
- The target user, narrow enough to be real.
- The single core thing the product must do.
- Explicit non-goals (what it deliberately won't do for v1).

Skip the ceremony if the idea is already crisp — say so and point to /assess.
Write the result into `ventures/<slug>/STATE.md` (or propose the slug if new).
Do not start building.
