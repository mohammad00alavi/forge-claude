---
description: Design tech + product architecture, pick the stack, and finalize the agent roster for a venture — $ARGUMENTS
---

Architect this venture: $ARGUMENTS

Hand off to the `architect` agent. It must:
1. Read STATE.md + the strategist's assessment.
2. Re-score difficulty against what's now known; announce any change.
3. Produce `ventures/<slug>/architecture.md`: justified stack, data model, key
   components, the hard parts, and (T2+) the feature breakdown as delta specs.
4. FINALIZE the agent roster — add/drop vs. tier default, justify each by its
   distinct output. Up-tier → strategist re-requests approval.

End with the Architecture verdict block. Ready for /forge after.
