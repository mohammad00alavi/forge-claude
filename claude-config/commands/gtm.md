---
description: Assemble the go-to-market package (positioning, landing copy, pricing, launch plan, sales assets) scaled to tier — $ARGUMENTS
---

Build the GTM package for: $ARGUMENTS

Hand off to `marketer` (+ `sales` at T3+), coordinated by `strategist`.
Produce real files in `ventures/<slug>/deliverables/`, scaled to tier:
- T0/T1: positioning + landing copy.
- T2: + pricing copy, basic launch plan.
- T3/T4: + launch sequence, ad/social drafts, sales narrative, pricing
  strategy, outreach templates, demo script.

Every deliverable gets a reviewer pass against
`references/business-quality.md` (the writer never grades its own copy).
Flag all pricing numbers and any claims needing verification for my sign-off.
Never publish or post anything — these are drafts for my review.
